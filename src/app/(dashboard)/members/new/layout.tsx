import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Add Member",
};

export default function NewMemberLayout({ children }: { children: React.ReactNode }) {
  return children;
}
