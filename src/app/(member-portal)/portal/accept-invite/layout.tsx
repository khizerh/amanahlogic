import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Accept Invite | Member Portal",
};

export default function AcceptInviteLayout({ children }: { children: React.ReactNode }) {
  return children;
}
